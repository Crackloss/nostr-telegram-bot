# Token del bot (obtener de @BotFather en Telegram)
BOT_TOKEN=tu_token_aqui

# ID del grupo de Telegram donde funcionará el bot
# Para obtenerlo: añade @userinfobot al grupo y escribe /start
# O usa la API: https://api.telegram.org/bot<TOKEN>/getUpdates
ALLOWED_CHAT_ID=0

# Ruta de la base de datos SQLite
DB_PATH=data/nostr_directory.db

# Activar push a GitHub (Fase 2, dejar en false por ahora)
GITHUB_PUSH_ENABLED=false
